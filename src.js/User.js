"use strict";

class User {
  constructor(name, firstName, age, email, password) {
    this.name = name;
    this.firstName = firstName;
    this.age = age;
    this.email = email;
    this.password = password;
  }
};
